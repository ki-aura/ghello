git tag --list
grep  "GHELLO_VERSION" ghello.h

git add .
git commit -m "ready for v1.3.0"
git push
git tag -a v1.3.0 -m "Release v1.3.0"
git push origin v1.3.0
gh release create v1.3.0 --title "Release v1.3.0" --notes "Release v1.3.0 notes..."
cd /opt/homebrew/Library/Taps/ki-aura/homebrew-ghello
curl -L -o v1.3.0.tar.gz https://github.com/ki-aura/ghello/archive/refs/tags/v1.3.0.tar.gz
shasum -a 256 v1.3.0.tar.gz
rm *.gz

bbedit Formula/ghello.rb

git add Formula/ghello.rb
git commit -m "ready for v1.3.0"
git push
